<?php if( count($this->invalidFields) > 0 ):
        $requiredClass = array();
        foreach($this->invalidFields as $k => $v):
            $fieldRequired[$v] = "fieldRequired";
        endforeach;
      endif;
?>